

# agentic-coding-claude-ch10-desktop

### Lab 2
Ah, that is completely my bad—I overcomplicated it in my last message! Looking at your screen, you don't actually need to change a dropdown toggle.

Because you are using the **Claude Desktop app** and it is already pointing to your local repository folder (`agentic-coding-claude... main`), **it is already running completely locally by default.**

To do parallel work right now from this exact screen:

### 1. For your first feature (Local)

You are good to go right here. Just type your prompt into the `Type / for commands` box:

> *"Move to a new worktree and build a simple countdown timer feature in timer.py."*

### 2. For your second feature (Parallel)

1. Click **`+ New session`** in the top left sidebar.
2. This opens a brand new, independent chat tab.
3. Type your second prompt in that new tab:

> *"Move to a new worktree and build a simple text-flipper feature in flipper.py."*

Claude will handle spinning up the separate local worktrees completely automatically for both sessions behind the scenes. No complex environment configuration required!